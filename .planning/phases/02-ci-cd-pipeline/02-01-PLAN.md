---
phase: 02-ci-cd-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .releaserc.json
  - commitlint.config.ts
  - .prettierrc
  - .prettierignore
  - .husky/pre-commit
  - .husky/commit-msg
  - package.json
autonomous: true
requirements:
  - CICD-01
  - CICD-02
  - CICD-03
  - CICD-04
  - CICD-05

must_haves:
  truths:
    - "Committing with a non-conventional message (e.g. 'added stuff') is rejected by the commit-msg hook"
    - "Staged .ts/.tsx files are linted and formatted before commit via the pre-commit hook"
    - "Semantic Release is configured for main (production), staging (rc prerelease), and dev (beta prerelease)"
    - "style: commits are mapped to patch releases; feat: to minor; fix/perf/refactor to patch; docs/chore/test/ci to no release"
    - "@semantic-release/changelog and @semantic-release/git plugins are wired in to update CHANGELOG.md and commit it"
  artifacts:
    - path: ".releaserc.json"
      provides: "Semantic Release configuration"
      contains: "branches"
    - path: "commitlint.config.ts"
      provides: "Commitlint rules"
      exports: ["default"]
    - path: ".prettierrc"
      provides: "Prettier formatting rules"
    - path: ".husky/pre-commit"
      provides: "Pre-commit hook running lint-staged"
    - path: ".husky/commit-msg"
      provides: "Commit-msg hook running commitlint"
  key_links:
    - from: ".husky/pre-commit"
      to: "lint-staged"
      via: "npx lint-staged"
      pattern: "lint-staged"
    - from: ".husky/commit-msg"
      to: "commitlint"
      via: "npx --no -- commitlint --edit"
      pattern: "commitlint"
    - from: "package.json lint-staged"
      to: "eslint + prettier"
      via: "staged file globs"
      pattern: "lint-staged"
---

<objective>
Configure Semantic Release, Commitlint, Husky hooks, lint-staged, and Prettier so local commits are quality-gated and pushes to main/staging/dev trigger versioned releases.

Purpose: Establishes the commit quality gate and release automation at the foundation of the CI/CD pipeline. Every subsequent workflow step depends on these contracts.
Output: .releaserc.json, commitlint.config.ts, .prettierrc, .prettierignore, .husky/pre-commit, .husky/commit-msg, lint-staged config in package.json
</objective>

<execution_context>
@/Users/danielvm/Sites/boilerplate-vercel-neon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/boilerplate-vercel-neon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ci-cd-pipeline/02-CONTEXT.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .releaserc.json and commitlint.config.ts</name>
  <files>.releaserc.json, commitlint.config.ts</files>
  <read_first>
    - package.json (confirm @semantic-release/changelog, @semantic-release/git, @commitlint/cli, @commitlint/config-conventional are in devDependencies)
    - .planning/phases/02-ci-cd-pipeline/02-CONTEXT.md (decisions D-01 through D-06, D-09)
  </read_first>
  <action>
Create `.releaserc.json` with exactly this content:

```json
{
  "branches": [
    "main",
    { "name": "staging", "prerelease": "rc" },
    { "name": "dev", "prerelease": "beta" }
  ],
  "plugins": [
    ["@semantic-release/commit-analyzer", {
      "preset": "conventionalcommits",
      "releaseRules": [
        { "type": "feat", "release": "minor" },
        { "type": "fix", "release": "patch" },
        { "type": "perf", "release": "patch" },
        { "type": "refactor", "release": "patch" },
        { "type": "style", "release": "patch" },
        { "type": "docs", "release": false },
        { "type": "chore", "release": false },
        { "type": "test", "release": false },
        { "type": "ci", "release": false }
      ]
    }],
    ["@semantic-release/release-notes-generator", {
      "preset": "conventionalcommits"
    }],
    ["@semantic-release/changelog", {
      "changelogFile": "CHANGELOG.md"
    }],
    ["@semantic-release/npm", {
      "npmPublish": false
    }],
    ["@semantic-release/git", {
      "assets": ["CHANGELOG.md", "package.json"],
      "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    "@semantic-release/github"
  ]
}
```

Create `commitlint.config.ts` with exactly this content:

```typescript
import type { UserConfig } from '@commitlint/types';

const config: UserConfig = {
  extends: ['@commitlint/config-conventional'],
};

export default config;
```
  </action>
  <verify>
    <automated>node -e "const r = require('./.releaserc.json'); const b = r.branches.map(b => typeof b === 'string' ? b : b.name); console.assert(b.includes('main') && b.includes('staging') && b.includes('dev'), 'branches missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .releaserc.json exists and contains `"main"`, `"staging"`, `"dev"` under `branches`
    - .releaserc.json contains `"style"` release rule with `"release": "patch"`
    - .releaserc.json contains `"feat"` with `"release": "minor"`
    - .releaserc.json contains `"@semantic-release/changelog"` and `"@semantic-release/git"` in `plugins`
    - .releaserc.json contains `"npmPublish": false` (project is private)
    - commitlint.config.ts exists and contains `@commitlint/config-conventional`
    - commitlint.config.ts contains `export default config`
  </acceptance_criteria>
  <done>Semantic Release is configured for 3-branch release strategy with correct commit-type → release-level mapping. Commitlint extends conventional config.</done>
</task>

<task type="auto">
  <name>Task 2: Create .prettierrc, .prettierignore, Husky hooks, and wire lint-staged in package.json</name>
  <files>.prettierrc, .prettierignore, .husky/pre-commit, .husky/commit-msg, package.json</files>
  <read_first>
    - package.json (confirm husky, lint-staged, prettier are in devDependencies; confirm `prepare` script is `husky`)
    - .planning/phases/02-ci-cd-pipeline/02-CONTEXT.md (decisions D-07, D-08, D-10, D-11)
  </read_first>
  <action>
Create `.prettierrc`:
```json
{
  "singleQuote": true,
  "trailingComma": "es5",
  "printWidth": 100,
  "tabWidth": 2
}
```

Create `.prettierignore`:
```
.next/
node_modules/
CHANGELOG.md
coverage/
```

Initialize Husky (creates the .husky directory):
```bash
npx husky init
```
This creates `.husky/pre-commit` with default content. Replace its content with:
```sh
npx lint-staged
```

Create `.husky/commit-msg`:
```sh
npx --no -- commitlint --edit "$1"
```

Add `lint-staged` configuration to `package.json`. Add this key at the top level of the JSON object (alongside `scripts`, `dependencies`, etc.):
```json
"lint-staged": {
  "*.{ts,tsx,js,jsx,mjs}": [
    "eslint --fix",
    "prettier --write"
  ],
  "*.css": [
    "prettier --write"
  ]
}
```

After creating the hook files, ensure they are executable:
```bash
chmod +x .husky/pre-commit .husky/commit-msg
```
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); const ls = p['lint-staged']; console.assert(ls && ls['*.{ts,tsx,js,jsx,mjs}'], 'lint-staged missing ts/tsx glob'); console.log('OK')" && test -f .husky/pre-commit && test -f .husky/commit-msg && echo "hooks exist"</automated>
  </verify>
  <acceptance_criteria>
    - .prettierrc exists and contains `"singleQuote": true`, `"trailingComma": "es5"`, `"printWidth": 100`, `"tabWidth": 2`
    - .prettierignore exists and contains `CHANGELOG.md` and `.next/`
    - .husky/pre-commit exists and contains `lint-staged`
    - .husky/commit-msg exists and contains `commitlint --edit`
    - package.json contains a top-level `lint-staged` key with `*.{ts,tsx,js,jsx,mjs}` glob
    - package.json `lint-staged["*.{ts,tsx,js,jsx,mjs}"]` array contains both `eslint --fix` and `prettier --write`
    - .husky/pre-commit is executable (`test -x .husky/pre-commit` exits 0)
    - .husky/commit-msg is executable (`test -x .husky/commit-msg` exits 0)
  </acceptance_criteria>
  <done>Prettier is configured. Husky hooks are in place. lint-staged runs eslint + prettier on staged TS/JS/CSS files before commit. commitlint enforces conventional commit messages on commit-msg.</done>
</task>

</tasks>

<verification>
After both tasks complete, validate the commit quality gate works end-to-end:

1. Verify commitlint rejects a bad message: `echo "added stuff" | npx commitlint` should exit non-zero
2. Verify commitlint accepts a valid message: `echo "feat: add login page" | npx commitlint` should exit 0
3. Verify prettier config is valid: `npx prettier --check package.json` (or any file) should not error on config parse
4. Verify .releaserc.json is valid JSON: `node -e "require('./.releaserc.json')" && echo valid`
</verification>

<success_criteria>
- `echo "added stuff" | npx commitlint` exits non-zero (invalid message rejected)
- `echo "feat: add feature" | npx commitlint` exits 0 (valid message accepted)
- .releaserc.json parses as valid JSON with 3 branches and 7 plugins
- package.json contains lint-staged config with eslint + prettier on staged TS/JS files
- .husky/pre-commit and .husky/commit-msg exist and are executable
</success_criteria>

<output>
After completion, create `.planning/phases/02-ci-cd-pipeline/02-01-SUMMARY.md`
</output>
