---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - SCAF-01

must_haves:
  truths:
    - "pnpm install completes without errors"
    - "All required tooling dependencies are listed in package.json"
    - "All required scripts are present in package.json"
    - "engines field constrains Node to 22.x and pnpm to 10.x"
  artifacts:
    - path: "package.json"
      provides: "Full dependency manifest with scripts and engines"
      contains: "vitest"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "pnpm install"
      pattern: "\"vitest\""
---

<objective>
Add all project dependencies, scripts, and engine constraints to package.json so the full toolchain (testing, linting, CI/CD, auth) is declared and installable.

Purpose: Every subsequent phase depends on these packages being available. This is the dependency manifest for the entire project.
Output: Updated package.json with all deps, all scripts, and engines block. node_modules populated via pnpm install.
</objective>

<execution_context>
@/Users/danielvm/Sites/boilerplate-vercel-neon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/boilerplate-vercel-neon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite package.json with all dependencies, scripts, and engines</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state — understand what already exists before overwriting)
  </read_first>
  <action>
Replace the entire contents of package.json with the following. Every field is a concrete value — do not guess or infer versions:

```json
{
  "name": "boilerplate-vercel-neon",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "type-check": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "prepare": "husky"
  },
  "dependencies": {
    "@neondatabase/auth": "^0.2.1",
    "@vercel/functions": "^2.0.0",
    "better-auth": "^1.2.7",
    "next": "16.2.1",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "@tailwindcss/postcss": "^4",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^4.3.4",
    "babel-plugin-react-compiler": "1.0.0",
    "commitlint": "^19.8.0",
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "eslint": "^9",
    "eslint-config-next": "16.2.1",
    "husky": "^9.1.7",
    "jsdom": "^26.1.0",
    "lint-staged": "^15.5.0",
    "prettier": "^3.5.3",
    "semantic-release": "^24.2.3",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^3.1.1"
  }
}
```

After writing the file, run: `pnpm install`

If pnpm install fails due to peer dependency conflicts, add `--legacy-peer-deps` is NOT a valid pnpm flag. Instead use `--no-strict-peer-dependencies` if needed. But try plain `pnpm install` first — React 19 + Next 16 should resolve cleanly.
  </action>
  <verify>
    <automated>cd /Users/danielvm/Sites/boilerplate-vercel-neon && pnpm install && node -e "const p=require('./package.json'); const required=['vitest','husky','prettier','semantic-release','@playwright/test','better-auth','@neondatabase/auth','@vercel/functions','lint-staged','commitlint']; const missing=required.filter(r=>!p.devDependencies[r]&&!p.dependencies[r]); if(missing.length){console.error('MISSING:',missing);process.exit(1)}else{console.log('All required packages present')}"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"vitest"` in devDependencies
    - package.json contains `"husky"` in devDependencies
    - package.json contains `"prettier"` in devDependencies
    - package.json contains `"semantic-release"` in devDependencies
    - package.json contains `"@playwright/test"` in devDependencies
    - package.json contains `"better-auth"` in dependencies
    - package.json contains `"@neondatabase/auth"` in dependencies
    - package.json contains `"@vercel/functions"` in dependencies
    - package.json contains `"lint-staged"` in devDependencies
    - package.json contains `"commitlint"` in devDependencies
    - package.json contains `"type-check"` script with value `"tsc --noEmit"`
    - package.json contains `"test:unit"` script with value `"vitest run"`
    - package.json contains `"test:e2e"` script with value `"playwright test"`
    - package.json contains `"prepare"` script with value `"husky"`
    - package.json contains `"engines"` block with `"node": ">=22.0.0"`
    - `pnpm install` exits 0
  </acceptance_criteria>
  <done>package.json has all required packages and scripts, pnpm install exits 0 with no errors</done>
</task>

</tasks>

<verification>
Run after task completes:

```bash
cd /Users/danielvm/Sites/boilerplate-vercel-neon
pnpm install
# Verify key packages installed:
ls node_modules/vitest
ls node_modules/husky
ls node_modules/prettier
ls node_modules/@playwright
ls node_modules/better-auth
```
</verification>

<success_criteria>
- `pnpm install` exits 0
- All 10 required packages present in package.json (vitest, husky, prettier, semantic-release, @playwright/test, better-auth, @neondatabase/auth, @vercel/functions, lint-staged, commitlint)
- scripts block includes: type-check, test:unit, test:unit:watch, test:e2e, test:e2e:ui, prepare
- engines block present with node >=22.0.0 and pnpm >=10.0.0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
